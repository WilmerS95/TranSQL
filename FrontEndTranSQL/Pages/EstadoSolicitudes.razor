@page "/estado-solicitudes"

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-10 col-md-12">
            <div class="card shadow-lg">
                <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                    <h3 class="card-title mb-0">
                        <i class="bi bi-file-earmark-check me-2"></i> Gestión de Estado de Solicitudes
                    </h3>
                    <button class="btn btn-outline-light" @onclick="CrearNuevaSolicitud">
                        <i class="bi bi-plus-circle me-1"></i> Nueva Solicitud
                    </button>
                </div>
                <div class="card-body">
                    <table class="table table-hover table-striped table-bordered align-middle text-center">
                        <thead class="table-dark">
                            <tr>
                                <th>#</th>
                                <th>Tipo de Solicitud</th>
                                <th>Fecha</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @* Aquí agregarás el código para iterar sobre las solicitudes *@
                            <tr>
                                <td>1</td>
                                <td>Reservación de Vehículo</td>
                                <td>2024-10-25</td>
                                <td>
                                    <span class="badge bg-success">Aprobada</span>
                                </td>
                                <td>
                                    <button class="btn btn-outline-primary btn-sm me-2" @onclick="VerDetalles">
                                        <i class="bi bi-eye"></i> Ver
                                    </button>
                                    <button class="btn btn-outline-danger btn-sm" @onclick="CancelarSolicitud">
                                        <i class="bi bi-x-circle"></i> Cancelar
                                    </button>
                                </td>
                            </tr>
                            @* Puedes iterar sobre una lista de solicitudes aquí *@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private void CrearNuevaSolicitud()
    {
        // Lógica para crear una nueva solicitud.
    }

    private void VerDetalles()
    {
        // Lógica para ver los detalles de una solicitud.
    }

    private void CancelarSolicitud()
    {
        // Lógica para cancelar una solicitud.
    }
}
